type User {
  name: String! # ! denotes required fields
  age: Int
  married: Boolean
}

type Query {
  getAllUsers: [User!]!
  getUsersByStatus(status: Boolean): [User]!
}

type Query {
  hello: String!
}
