type User {
  name: String! # ! denotes required fields
  age: Int
  married: Boolean
}

type Query {
  getAllUsers: [User!]!
  # TODO add query for getting users by marriage status
}

type Query {
  hello: String!
}
