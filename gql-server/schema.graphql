type User {
  name: String! # ! denotes required fields
  age: Int
  married: Boolean
}

type Query { # TODO(goal2) add query for getting users by marriage status
  getAllUsers: [User!]!
  hello: String!
}
# TODO(goal2) add query for getting users by marriage status
# TODO(goal3) add mutation for creating a user
